<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- SQL 문장을 찾을 때 사용할 그룹명을 설정한다. 
     보통 그룹명은 SQL 맵퍼 파일이 있는 경로를 그룹명으로 지정한다.
     또는 SQL을 사용할 인터페이스나 클래스 경로를 그룹명으로 지정한다.
     물론 어떤 이름으로 지정해도 상관없지만, 가능한 위의 규칙을 준수하여 
     유지보수의 일관성을 유지하라!-->
<mapper namespace="step25.ex5.BoardDao">

  <!-- SQL문은 태그 안에 작성한다.
       <select> 태그에는 select 문장을, 
       <insert> 태그에는 insert 문장을,
       <update> 태그에는 update 문장을,
       <delete> 태그에는 delete 문장을 작성한다.
       그런데 사실상 아무 태그를 사용해도 된다.
       어차피 SQL문을 찾을 때 id 속성 값으로 찾기 때문이다.
       그럼에도 불구하고 유지보수의 일관성을 위해 
       SQL 문의 따라 적절한 태그를 사용하라!
   -->
   
  <!-- select 태그
       id : SQL문을 찾을 때 사용할 식별자이다.
       resultType : 결과를 저장할 클래스이다. 
       반드시 fully-qualified name을 사용하라! -->
  <select id="selectBoard" resultType="step25.ex5.Board">
    
  </select>
</mapper>







